#include <bits/stdc++.h>

using std::cin;
using std::cout;

const char endl = '\n';

int main() {
    std::ios::sync_with_stdio(false);
    cin.tie(nullptr), cout.tie(nullptr);

    cout << "Hello,World!" << endl;

    return 0;
}